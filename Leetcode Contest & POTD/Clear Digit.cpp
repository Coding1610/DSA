#include<bits/stdc++.h>
using namespace std;

/*

class Solution {
public:
    string clearDigits(string s){
 
        string newS = "";
 
        for( char ch : s ){
            if( isdigit(ch) ){
                if( !newS.empty() ) newS.pop_back();
            }
            else{
                newS.push_back(ch);
            }
        }

        return newS;
        
    }
};

*/