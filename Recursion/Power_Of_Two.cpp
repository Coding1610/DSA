#include<bits/stdc++.h>
using namespace std;

/*
class Solution {
public:
    bool isPowerOfTwo(int n) {

        // Base Case
        if( n == 1 ){
            return true;
        }

        if( n%2 != 0 || n <= 0 ){
            return false;
        }

        // Recursion Call
        return isPowerOfTwo(n/2);
    }
};
*/