#include<bits/stdc++.h>
using namespace std;

/*
int maxPrefix(int k, vector<int> a)
{
    sort(a.begin(), a.end(), greater<int>());
    int sum=0;
    for(int i=0;i<k;i++){
        sum+=a[i];
    }
    return sum;
}
*/