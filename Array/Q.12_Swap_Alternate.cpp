#include<bits/stdc++.h>
using namespace std;

/*
void swapAlternate(int *arr, int size)
{   
    int last = aarr[size-1];
    for( int i = 0 ; i < size ; i+=2 ){
        swap(arr[i],arr[i]+1);
        if( arr[size-1] == 0 ){
            arr[size-1] = last;
        }
    }
}
*/