#include<bits/stdc++.h>
using namespace std;

/*
long long int sumOrProduct(long long int n, long long int q)
{
	long long int sumOrProduct = 0;

	if( q == 1 ){
		for( int i = 1 ; i <= n ; i++ ){
			sumOrProduct = sumOrProduct+i;
		}
	}

	else{
		sumOrProduct = 1;
		for( int i = 1 ; i <= n ; i++ ){
			sumOrProduct = (sumOrProduct*i)%1000000007;
		}
	}

	return sumOrProduct;
}
*/