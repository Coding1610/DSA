#include<bits/stdc++.h>
using namespace std;

/*

class Solution {
public:
    int passThePillow(int n, int time){

        int direction = 1;
        int index = 1;

        while( time != 0 ){

            if( index == n ){
                direction = -1;
            }

            if( index == 1 ){
                direction = 1;
            }

            time--;
            index += direction;
            
        }

        return index;
        
    }
};

*/