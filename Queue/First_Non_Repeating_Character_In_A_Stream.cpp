#include<bits/stdc++.h>
using namespace std;

/*

char firstNonRepeatingCharacter(string str) {

  unordered_map<char,int> mapp;

  for( char ch : str ){
    mapp[ch]++;
  }

  for( int i = 0 ; i < str.size() ; i++ ){
    if( mapp[str[i]] == 1 ){
      return str[i];
    }
  }

  return str[0];

}

*/