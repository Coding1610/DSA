#include<bits/stdc++.h>
using namespace std;

/*
class Solution {
public:
    int lengthOfLastWord(string s) {
        
        int length = 0;
        bool space = false;

        for( int i = s.length()-1 ; i >= 0 ; i-- ){

            if( s[i] != ' ' ){
                space = true;
                length++;
            }

            else if(space){
                break;
            }
        }
        return length;
    }
};
*/