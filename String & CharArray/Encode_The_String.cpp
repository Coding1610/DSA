#include<bits/stdc++.h>
using namespace std;

/*

string encodeString(string &str, int n) {

    for (int i = 0; i < str.length(); i++) {

        if (str[i] == 'a' || str[i] == 'e' || str[i] == 'i' || str[i] == 'o' || str[i] == 'u') {
            str[i] = (str[i] + 1);
        } 
        else {
            str[i] = (str[i] - 1);
        }

    }
    return str;
}

*/