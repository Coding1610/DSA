#include<bits/stdc++.h>
using namespace std;

/*

class Solution {
public:
    bool doesValidArrayExist(vector<int>& derived) {
        
        int x = 0;
        
        for( int bit : derived ){
            x ^= bit;
        }

        return ( x == 0 ? true : false );
    
    }
};

*/