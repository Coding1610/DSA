#include<bits/stdc++.h>
using namespace std;

/*

class Solution {
public:
    bool checkTwoChessboards(string coordinate1, string coordinate2) {
        
        int i1 = (coordinate1[0]-'a')+1;
        int i2 = (coordinate2[0]-'a')+1;

        int n1 = coordinate1[1] - '0';
        int n2 = coordinate2[1] - '0';

        if( ( (i1+n1)%2 == 0 ) && ( (i2+n2)%2 == 0 ) ) return true;

        if( ( (i1+n1)%2 != 0 ) && ( (i2+n2)%2 != 0 ) ) return true;

        return false;

    }
};

*/