#include<bits/stdc++.h>
using namespace std;

/*
class Solution {
public:
    bool isPowerOfFour(int n) {

        // Base Case
        if( n == 1 ){
            return true;
        }

        if( n%4 != 0 || n <= 0 ){
            return false;
        }

        // Recursion Call
        return isPowerOfFour(n/4);
    }
};
*/