#include<bits/stdc++.h>
using namespace std;

/*
class Solution {
public:
    bool isPowerOfThree(int n) {

        // Base Case
        if( n == 1 ){
            return true;
        }

        if( n%3 != 0 || n <= 0 ){
            return false;
        }

        // Recursion Call
        return isPowerOfThree(n/3);
    }
};
*/