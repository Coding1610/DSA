#include<bits/stdc++.h>
using namespace std;

/*
int main() {

    int size;
    cin >> size;

    int arr[size];
    for(int i=0; i<size; i++){
        cin >> arr[i];
    }

    // Algorithm 1
    reverse( arr , arr+size );
    
    // Algorithm 2
    for( int i = 0 ; i < size/2 ; i++ ){
        swap(arr[i],arr[size-i-1]);
    }

    // Algorithm 3
    int s  = 0;
    int e = size-1;
    while( s <= e ){
        swap(arr[s],arr[e]);
        s++;
        e--;
    }

    for( int i = 0 ; i < size ; i++ ){
        cout << arr[i] << " ";
    }
}
*/