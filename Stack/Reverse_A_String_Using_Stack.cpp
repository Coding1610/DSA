#include<bits/stdc++.h>
using namespace std;

/*
 
string reverseString(string str)
{   
    string ans = "";
    stack<char> st;

    for( int i = 0 ; i < str.size() ; i++ ){
        char ch = str[i];
        st.push(ch);
    }

    while( !st.empty() ){
        char ch = st.top();
        st.pop();
        ans.push_back(ch);
    }

    return ans;
}

*/