#include<bits/stdc++.h>
using namespace std;

/*

class Solution {
public:
    vector<string> buildArray(vector<int>& target, int n){

        int index = 0;
        vector<string> ans;

        for( int i = 1 ; i <= n && index < target.size() ; i++ ){
            
            ans.push_back("Push");

            if( i != target[index] ){
                ans.push_back("Pop");
            }

            else{
                index++;
            }

        }
        return ans;
    }
};

*/